import { Switch } from "@jamsrui/react";

export const SwitchUsage = () => {
  return (
    <Switch label="Press Me" errorMessage="You must select an option">
      A Switch component allows users to toggle between two states, typically
      representing an on/off or active/inactive choice. It is commonly used for
      enabling or disabling features in a user interface.
    </Switch>
  );
};
